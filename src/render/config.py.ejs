worlds['My World'] = <%= JSON.stringify(world) %>

outputdir = <%= JSON.stringify(output) %>

rendermode = "lighting"

renders["render1"] = {
        'world': 'My World',
        'title': 'A regular render'
}
